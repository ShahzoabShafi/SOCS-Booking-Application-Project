# SOCS-Booking-Application-Project

- Frontend team works **only inside** `frontend/` on the `dev-frontend` branch
- Backend team works **only inside** `backend/` on the `dev-backend` branch

When they merge into `development`, Git keeps the folders separate cleanly with **zero conflicts** since both teams are never touching the same files.
